import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SavingScreen = () => {
  return (
    <View>
      <Text>Savign Screen</Text>
    </View>
  );
};

export default SavingScreen;

const styles = StyleSheet.create({});
